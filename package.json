{
  "name": "@kotoba-lang/envelope",
  "private": true,
  "description": "Dev-only: npm deps for the CLJS half (X25519 via @noble/curves, through kotoba-lang/org-signal).",
  "devDependencies": {
    "@noble/curves": "^1.9.0",
    "@noble/hashes": "^1.8.0"
  },
  "scripts": {
    "test": "nbb --classpath \"src:test:../org-signal/src\" scripts/run-tests.cljs"
  }
}
